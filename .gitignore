# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
web/dist/
*.tsbuildinfo

# Env & secrets
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test / coverage
coverage/
.vitest/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
*.swp
*~

# CWCC runtime data (never commit user runtime state or tokens)
.claude/cwcc/
token
stats.json

# AI assistant / agent artifacts — keep the repo free of anything an agent generates.
# Covers Claude Code config & permissions, agent-authored working docs, and scratch files.
.claude/
CLAUDE.md
AGENTS.md
.cursor/
.aider*
.github/copilot-instructions.md

# Agent-authored working docs (notes, logs and hand-offs — not part of the package)
CHANGELOG.md
PROGRESS.md
QA.md
NOTES.md
TODO.md
how_to_go_prod.md

# Packaging artifacts (`npm pack` output — never commit)
*.tgz

# Backups & scratch left behind by tooling
*.bak
*.orig
*.rej
*.tmp
.scratch/
scratchpad/
